# CPU-only PyTorch wheels keep the image small. Drop the extra index URL if you
# want CUDA/GPU builds instead. transformers 4.44+ is required for newer model
# families (Gemma 2, Qwen2.5, etc.).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
transformers==4.44.2
accelerate==0.33.0
safetensors==0.4.4
sentencepiece==0.2.0
einops==0.8.0
